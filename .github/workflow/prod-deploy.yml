name: Deploy to PROD - Azure Container Apps

on:
  push:
    branches:
      - main

jobs:
  deploy-prod:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Login to Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Docker Login to ACR
        run: |
          echo ${{ secrets.ACR_PASSWORD }} | docker login ${{ secrets.ACR_LOGIN_SERVER }} \
          --username ${{ secrets.ACR_USERNAME }} --password-stdin

      - name: Build Production Image
        run: |
          docker build -t ${{ secrets.ACR_LOGIN_SERVER }}/nextjs-app:prod-${{ github.sha }} .

      - name: Push Production Image
        run: |
          docker push ${{ secrets.ACR_LOGIN_SERVER }}/nextjs-app:prod-${{ github.sha }}

      - name: Deploy to Azure Container Apps (PROD)
        run: |
          az containerapp update \
            --name ${{ secrets.PROD_CONTAINER_APP }} \
            --resource-group ${{ secrets.RESOURCE_GROUP }} \
            --image ${{ secrets.ACR_LOGIN_SERVER }}/nextjs-app:prod-${{ github.sha }}
